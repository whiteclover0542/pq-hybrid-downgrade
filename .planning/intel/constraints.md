# Constraints (from SPECs)

No SPEC-type documents were ingested in this run. No API contracts, schemas, NFRs, or protocol constraints extracted as canonical SPECs.

Note: PROPOSAL.md (classified PRD) contains SPEC-adjacent material — a system diagram, a testbed target table, fault-injection methods, and an observation-metric table — but the classifier judged these as proposal content, not binding contracts (no API/schema/protocol contracts or SLOs). These are captured as requirements (see requirements.md) and context (see context.md), not as constraints.

Source classifications scanned: 9 (1 PRD, 8 DOC). SPEC count: 0.
